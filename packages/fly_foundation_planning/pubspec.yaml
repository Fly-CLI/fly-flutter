name: fly_foundation_planning
description: Shared planning library for Fly foundation template generation
version: 1.0.0

environment:
  sdk: ">=3.5.0 <4.0.0"
  flutter: ">=3.10.0"

resolution: workspace

dependencies:
  path: ^1.9.0

dev_dependencies:
  test: ^1.24.0

