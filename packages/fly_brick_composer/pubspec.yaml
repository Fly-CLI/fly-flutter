name: fly_brick_composer
description: Shared brick composition library for Fly template composition
version: 1.0.0

environment:
  sdk: ">=3.8.0 <4.0.0"
  flutter: ">=3.10.0"

resolution: workspace

dependencies:
  path: ^1.9.0

dev_dependencies:
  test: ^1.24.0

