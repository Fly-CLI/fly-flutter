{{#is_service}}
{{#with_tests}}
import 'package:flutter_test/flutter_test.dart';

import '../../../../lib/core/services/{{feature}}/{{component_name}}_service.dart';

void main() {
  test('{{component_name.pascalCase()}}Service returns success result', () async {
    final service = {{component_name.pascalCase()}}Service();

    final result = await service.fetchSummary();

    expect(result.isSuccess, isTrue);
    expect(result.data?['service'], '{{component_name}}');
  });
}
{{/with_tests}}
{{/is_service}}

