name: fly_mcp
description: MCP (Model Context Protocol) package for Fly CLI – includes core primitives, server implementation, tools, resources, and prompts.
version: 0.1.0
publish_to: "none"

environment:
  sdk: ">=3.3.0 <4.0.0"

dependencies:
  dart_mcp: ^0.3.3
  meta: ^1.12.0

dev_dependencies:
  lints: ^4.0.0
  test: ^1.25.0

