name: fly_foundation_feature
description: Fly foundation feature scaffolding brick for generating feature screens and view models for existing Fly projects
version: 1.0.0
type: feature

# Variables are passed programmatically by the CLI orchestrator
# This brick generates feature screens and view models for existing projects
# For usage, see: https://github.com/fly-cli/fly

